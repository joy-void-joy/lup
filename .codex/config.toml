# Generated from lup.adapters.codex.harness by `uv run lup-devtools harness generate all` — edit the source, not this file.
# See docs/generated-artifacts.md.
# Personal sandbox and approval defaults stay in ~/.codex/config.toml.
# Native shell allows are generated under .codex/rules/.
# Tool servers start from this project, so start the runtime at its root.

project_doc_max_bytes = 32768

[features]
hooks = true

[mcp_servers.notes]
command = "uv"
args = ["run", "--directory", ".", "lup-devtools", "agent", "serve-tools", "--server", "notes", "--session", "harness"]

[mcp_servers.sandbox]
command = "uv"
args = ["run", "--directory", ".", "lup-devtools", "agent", "serve-tools", "--server", "sandbox", "--session", "harness"]

[mcp_servers.codeintel]
command = "uv"
args = ["run", "--directory", ".", "lup-devtools", "agent", "serve-tools", "--server", "codeintel", "--session", "harness"]
