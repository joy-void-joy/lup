# Generated file — do not edit directly. Rendered from
# lup.adapters.codex.harness by `uv run lup-devtools harness generate all`.
# Personal sandbox and approval defaults stay in ~/.codex/config.toml.
# Native shell allows are generated under .codex/rules/.
[features]
hooks = true
