# Generated from lup_template.devtools.dev.workflow by `uv run lup-devtools harness generate all` — edit the source, not this file.
# See docs/generated-artifacts.md.

name: Quality

on:
  pull_request:
  push:
    branches: [main, dev]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv sync --all-extras
      - name: Quality gate
        run: uv run lup-devtools dev check
