| Downstream template guidance | `.claude/plugins/lup/TEMPLATE_CLAUDE.md` from `content/template_claude.py` | `.codex/plugins/lup/TEMPLATE_AGENTS.md` from `content/template_codex.py` | Both flavors compose the portable sections in `content/template_sections.py`; only platform slices (guidance-file names, meta-agent naming, native edit and approval guidance, LSP vs CLI diagnostics, settings, communication idiom) differ. |
| Launch and trust | Launches the verified local plugin directory with `--plugin-dir`; `CLAUDE_CONFIG_DIR` selects the profile | Seeds a persistent per-worktree home from personal authentication and settings, materializes the Claude daltonized theme without selecting it, and publishes a verified content-addressed plugin revision; explicit `--codex-home`/`CODEX_HOME` overrides bypass isolation | Codex installs in a temporary home, verifies the native output, then publishes the revision and only its marketplace/plugin registration. Native installation never runs against the live home: its cache pruning would remove earlier revisions. Concurrent Lup publishers serialize, unrelated settings survive, and existing revision paths remain available. |
| A launched session's coordination identity | The durable id is exported as `LUP_COORDINATION_MEMBER`, and `--name <worktree>` makes the runtime's own display agree with the roster. A session nobody launched joins under the id Claude Code gave the process instead: its stdio tool servers find it as `CLAUDE_CODE_SESSION_ID` in their environment and its hooks read the same value on stdin as `session_id`, so the server that beats and the hook that departs name one row | The same variable, exported by the same code; the launch takes no name flag, so nothing is displayed by the runtime. A session nobody launched joins under nothing: Codex documents no session id reaching a server it starts, so its tool server serves no coordination verbs, and its hooks fold the record and beat without ever joining a row | Both halves are minted in `session_argv`, the one place either launcher passes through, so the id is a fact about having been launched rather than about which CLI was. The unlaunched fallback is each adapter's own answer, asked through `lup.providers.identity` by runtime name, so the variable's spelling stays where the runtime that sets it is spelled. On Claude Code it is measured rather than documented — read off a running coordination server under 2.1.272, while the env-vars and hooks pages document `CLAUDE_PROJECT_DIR` for servers and neither session variable — and a release that stopped setting it would cost an unlaunched session its identity and a launched one nothing. On Codex the absence is documented: hooks receive `session_id` (https://learn.chatgpt.com/docs/hooks) and a server's `env`/`env_vars` name variables to set or forward (https://learn.chatgpt.com/docs/extend/mcp), none carrying the session's id. Measured against Codex 0.153.4, whose launch options are `-c/--config`, `--enable`, `--disable`, `--remote`, `--remote-auth-token-env`, `--strict-config`, `-i/--image`, `-m/--model`, `--oss`, `--local-provider`, `-p/--profile`, `-s/--sandbox`, `--approve-for-me`, the two `--dangerously-` forms, `--add-dir`, `-a/--ask-for-approval`, `--search` and `--no-alt-screen` — none of which names a session. The difference costs a peer nothing: what addressing resolves through is lup's own `names.jsonl`, so the roster answers to the same derived worktree name on both runtimes and `dev coordination rename` changes it on both. Only the CLI's own chrome differs, and only on the runtime that has chrome to put it in. |
| Mail reaching a peer mid-session | A second `PreToolUse` group whose matcher is empty, so it fires before every tool: a shell guard comparing the mailbox's length to this member's delivered position, handing over to a verbatim `delivery_runtime` only where it has grown | Nothing extra; the policy matcher already names the tool every read goes through | The gap is one runtime's, because the tool rosters differ rather than the delivery does. Claude reads through native `Read`/`Grep`/`Glob`, which the policy matcher does not name and must not: every entry in `routed_tools` is proved to have a branch deciding it, so naming `Read` there would make `Read` a tool a policy could refuse. Codex "reads a document only by running a shell command over it", and `Bash` is already routed — so a peer there hears everything at the same moments without a second artifact. Both halves are measured rather than assumed: `PreToolUse` was observed firing for `Read`, `Grep`, `Glob` and `SendMessage` under an empty matcher, which the vendor documents for none of them. The guard costs 7.85ms per call with nothing waiting, against 107ms for the dispatcher, and fails open at every step — the opposite of the policy guard, because a permission that cannot be decided must not be granted, while mail that cannot be read must not stop the work it was meant to inform. |
| The roster reaching a session at prompt time | A `UserPromptSubmit` group carrying a shell guard that exits where this repository has no roster and otherwise hands over to the `coordination_changes` entry beside the shipped `coordination` package, whose `changes` fold's `hookSpecificOutput.additionalContext` is what changed since this session's last prompt: contested paths under this checkout first, then paths a peer holds here, arrivals, departures and redescriptions, with a one-line pointer to `coordination_peers` on the first prompt and nothing at all when nothing moved | The same guard and the same fold under the same event name, `UserPromptSubmit`, which Codex documents beside `SessionStart`, `SessionEnd`, `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PreCompact`, `PostCompact`, `SubagentStart`, `SubagentStop`, `Stop` and `Interrupt` | One rendering rather than two, because the adapters own two words — the event's name and the variable the plugin root is exported as — and the fold lives in `lup.coordination.bare.changes`, over the one store fold beside it, rendered by `lup.providers.roster_prompt` wherever a `peer_policy` is declared. Both vendors document the same input (`session_id`, `cwd`, `prompt`, `hook_event_name`) and the same output shape read from stdout on exit 0: Claude Code at https://code.claude.com/docs/en/hooks, Codex at https://learn.chatgpt.com/docs/hooks, where https://developers.openai.com/codex/hooks redirects, adding that `matcher` is not read for this event and that `additionalContext` is capped near 2,500 tokens per hook by default, past which it spills to disk — a fold of a few lines never nears it. The evidence differs by runtime and is stated as such: on Claude Code the rendered guard's stdout is measured by a test that runs it over a store the typed writers produced, and its delivery in a live session is not yet measured; on Codex the event, its fields and its context channel rest on that documentation alone, since a contained session reaches no Codex login and a nested `codex exec` there answers 401, the credential sitting outside what a session is granted — so the run that would measure it is the operator's to start from a host terminal. What a session hears *between* prompts is unchanged by this row: mail arrives through the delivery hook on Claude and through the routed shell tool on Codex, and an idle Codex session can be nudged by `coordination watch --as-run` through `codex queue`, the path it declares — a wake on top of the record, never the roster's changes, and the watcher's business rather than this hook's. |
| A session's presence, and its departure | Two signals. The coordination tool server beats for the session it serves — one file per member under the store, touched every 30 seconds for as long as the server runs, each tick sweeping the rows whose pulse stopped and joining its own again — so a session that was killed or whose container stopped reads as gone within two minutes, and its held paths and its redirectable address go with it. A `SessionEnd` group carrying the same shell guard as the prompt hook hands over to the `coordination_departure` entry, which appends the session's finish through the same store fold every reader folds it with, so a clean exit, a cleared conversation and a termination signal end the row at once; the prompt hook beats too, at each prompt | The same server, the same beat, and the same `SessionEnd` guard and writer, under the event name Codex documents beside `SessionStart` | One rendering per signal, in `lup.coordination.bare.store`, registered by `lup.providers.roster_prompt` wherever a `peer_policy` is declared. Presence is read three ways and the rule is one *because it is one function*: the typed listing, the prompt-time fold and the compiled permission dispatcher all call that module's `present`, which reads a session as here only while its newest record or its latest beat is within the window that module declares — so a rule the three once kept separately can no longer be kept differently. The design leans on no ending being exact: a finish the process outlives is undone by the server's next tick, which joins again, and a real exit stops the ticks, so that finish stands. Claude Code documents `SessionEnd` at https://code.claude.com/docs/en/hooks-guide with `session_id`, `cwd` and a `reason` among `clear`, `resume` (the session saved for resumption), `logout`, `prompt_input_exit` and `other`, and documents that `/clear` fires the session events while MCP servers stay connected — which is the case the re-join covers; what a checkpoint rewind or an interrupted turn fires is not documented, and nothing here depends on it. Nothing fires on a kill the process cannot catch, which is what the pulse is for. On Codex the event rests on its documentation alone, as the prompt hook's does. |
| A subagent's background work at its report | A `SubagentStart` group and a `SubagentStop` group, each carrying a shell guard that hands over to the `subagent_cleanup` host half beside the shipped kernel. At the start the fold's `hookSpecificOutput.additionalContext` is one sentence: what the subagent arms in the background is its own to stop with `TaskStop` before it reports. At the stop the fold reads the payload's `background_tasks`, which is the whole session's, and the subagent's own transcript at `agent_transcript_path`; a listed task is the subagent's when its command is one a `Monitor` or backgrounded `Bash` call there armed, or its description and type are what an `Agent` call there started, and the subagent's own entry is never its to stop. While any such task is listed the report is refused once, as a block decision whose reason names each task's id and the call that ends it; the stop that follows carries `stop_hook_active` and goes through | Nothing registered yet. Codex documents both events and the same block envelope, and hands `agent_transcript_path` with no task list, so the same judgement would read the subagent's rollout for a shell session left open. Measured on 0.155.1: the subagent ran its loop through `Bash` and was handed a session id, and after the Codex session ended `pgrep -af 'echo tick'` found nothing on the host, so a leftover there lives at most as long as the session and, since the parent awaits its subagent rather than backgrounding it, re-wakes nothing. No gate is registered until a run shows a session outliving its subagent *within* a Codex session | The judgement is `lup.policy.kernel.subagents`, compiled into both trees; the events, the payload's keys, the transcript's shape and the tool names are the host half's, `lup.providers.claude.assets.subagent_cleanup`, registered by `lup.providers.subagent_cleanup` wherever the hook set declares `subagent_cleanup`, which it does by default. Measured on Claude Code 2.1.278 and kept as fixtures under `tests/unit/fixtures/subagent_cleanup/`: a background subagent's monitor survives its report and each line it emits resumes the finished subagent, which the runtime's own notification describes as "may resume on its own when that work completes or reports"; the stop-time list types a monitor and a backgrounded command alike as `shell`, holds the subagent's own entry, and marks nothing as anybody's; a block on the first stop made the subagent `TaskStop` its monitor within seconds, and the second stop carried the flag and an emptied list. In auto mode, where the report is a `SubagentHandback` call, the subagent's first stop still precedes that call, so the block lands before the report and exactly one report follows the cleanup. The main agent is deliberately not gated: its `Stop` fires with background subagents listed as running, and that wait is wanted. On Codex, measured on 0.155.1 in a user-run session: the parent awaits a subagent through `collaborationwait_agent` rather than backgrounding it, the subagent's tool calls carry `agent_id` and `agent_type` although the docs say nothing of it, its shell call was handed a session id, and whether that session outlived it is the check the Codex half waits on. |
| A native call that would reach a peer | `SendMessage` and `ListAgents` are routed and decided. A send whose input names a live member of this repository's roster is denied, with the durable surface named rather than only refused; every other target passes through untouched, which is what leaves subagent continuation and every session this repository does not hold working exactly as before. A listing decides nothing and carries the roster in `additionalContext` beside it | Nothing extra: no call in its roster addresses another session, so there is none to route | One runtime's tool roster differs, not the judgement — which is settled once in `lup.policy.kernel.peers`, compiled into both trees, and reached through host-folded spellings because the kernel resolves nothing itself. Every string the call carries is matched rather than a named field, for the reason a refusal is: which key spells a recipient is the runtime's own business, and a check knowing one spelling fails open on the rest. The listing is deliberately not refused. Measured against a live account, most of what it returns is sessions in *other* repositories, which a repository-scoped roster is structurally incapable of holding — so a redirect would answer a question the reader did not ask, and would be escalated past every time. Attaching rather than deciding is why the verdict is `defer`: lup grants nothing it was not asked to grant, and the runtime's own permissions still settle a call it says nothing about. `additionalContext` on `PreToolUse` is a documented field whose delivery moment the vendor leaves unstated, so the attachment fails open — a roster that never arrived would cost a reader one attachment and nothing else. Measured in a live session on 9 September 2026: the hook's context reached the model **in the same turn as the tool result**, labelled as the hook's additional context beside the listing, and was readable before the model acted on either. The listing that came with it named twenty sessions, every one in another repository and offline, which is the population a repository roster cannot hold and the reason the call is attached to rather than refused. |
| A spawn without a name | `Agent` is routed and decided: a call whose input carries no `name`, or a blank one, is denied with the reason and the recovery the hook set's `spawn_names` declares — the shape a name takes and what it is for — and a call carrying one is deferred to the runtime's own permissions. An escalation marker in any of the call's strings turns the denial into the approval question | `collaborationspawn_agent` is routed and decided the same way over `task_name`, the field its spawn carries | One judgement, `lup.policy.kernel.spawns`, compiled into both trees; which tool spawns and which field names the spawn are each host half's. Measured on Claude Code 2.1.278: a subagent is listed, addressed and stopped by the `name` its spawn carried, and by its type where none was, so a session with several general-purpose subagents shows several rows saying the same thing; the tool accepts `name` and validates its spelling although the schema shown to the model omits the parameter, which is why a model passes one only when told to, and the recovery tells it. Measured on Codex 0.155.1 in a user-run session: the spawn carried `task_name` and the hook named the tool `collaborationspawn_agent`, so the rule there insists on what the runtime already requires and never fires in practice; it is routed all the same so both trees decide the call, rather than one deciding and the other passing it by. Deferred rather than allowed on both, because the kernel grants nothing it was not asked to grant. |
| Waking an idle peer | Nothing this library can run. Claude Code's CLI offers `agents`, `attach`, `logs`, `respawn`, `rm` and `stop`, and no verb that speaks to a running session — so `wake()` hands the caller an instruction naming its own `SendMessage` tool and the address to use | `codex queue --thread <uuid or name> --message <text>`, which reaches a session from any process on the machine, so `wake()` finishes the job itself | The asymmetry is the runtime's and is carried rather than papered over: a wake is always *on top of* the mail, never instead of it, so the record is identical on both and only the latency differs. Measured against both CLIs' own `--help`. The handle is self-reported on the roster because on Claude it can only be — `CLAUDE_CODE_SESSION_ID` and `CLAUDE_CODE_BRIDGE_SESSION_ID` are both set and neither is what peers address the session by, which `ListAgents` reports and nothing in the environment carries. A member that declared no handle is told so rather than silently un-nudged, which is the third honest outcome and the one a parked task lands in. What `codex queue` does to an *idle* session — wakes it, or queues for its next turn — is not yet measured: `codex-cli 0.153.4` documents the verb as queueing for an existing session and no Codex session was live to observe, so `wake()` reports `reached` on a zero exit, which is the honest reading of the command having succeeded and not of the peer having looked. Settling it takes one idle Codex session, one `coordination watch --nudge`, and a look at whether the session's transcript advances before its next prompt. |
| Runtime preflight | `claude` CLI version, plugin support, `plugin validate` | CLI version, cache digest, and native `account/read` with managed-token refresh | Account checks use the session's execution boundary: the selected host home for inner/none, the container's home for outer. Container sign-in uses device authentication and is checked again afterward. Named profiles remain explicitly unverified because the account API cannot select them. Declining sign-in continues explicitly unverified; account readiness does not prove implicit MCP service startup. |
| Reasoning effort | `CLAUDE_EFFORT` in `providers/claude/selection.py` | `CODEX_EFFORT` in `providers/codex/selection.py` | `SessionRequest.effort` is asked for in portable words and mapped by each adapter, the way autonomy already is. `low`, `medium`, `high`, and `xhigh` are the four rungs both ladders carry outright. The two ends belong to one runtime each and the other renders the nearest it has: `minimal` is Codex's own floor, which Claude meets with `low` because its ladder has no rung beneath that; `max` is Claude's own ceiling, which Codex meets with `xhigh` for the same reason at the top. Codex's `none` is deliberately absent from the portable vocabulary — Claude would render it as `low`, turning "do not reason" into "reason a little" on one runtime without saying so. |
| Usage display | the OAuth usage endpoint for live windows, plus the local stats cache for per-day and per-model detail | the app-server's own account calls for both the metered windows and the daily token buckets | One display over two readers (`lup.observability.usage`, `usage/reader.py` in each adapter). Each side reports a plan's windows and its days into the same report, so the pacing bars, the daily budget, and the `--json` snapshot are decided once. What differs is what each account publishes: fixed named windows and a per-model split on one side, two self-describing windows and no model breakdown on the other — which is why one draws a model legend and the other has none to draw. |
| Sensitive local-only files | `.claude/settings.local.json` | `.codex/config.local.toml` | Native personal-config locations, excluded from generation. |
| Surfacing a verdict's evidence to the approver | The reason is rendered in the prompt for a shell command and dropped in the create-file dialog, so a verdict that enumerated sites repeats them in `systemMessage`, coloured for this terminal (`announced` in `providers/claude/assets/policy_dispatcher.py`) | Nothing extra: the dispatcher already writes `decision.reason` to stderr, which is shown whole | Measured against Claude Code 2.1.237, both directions. `permissionDecisionReason` reaches the person for `Bash` and not for `Write`/`Edit`; `systemMessage` reaches them from every hook but arrives with the tool call rather than with the prompt, so on this runtime the evidence informs rather than gates. `PermissionRequest` — the event that runs before the prompt — belongs to the control protocol rather than to a local plugin and never fires for one, which is why Codex's declaration names it and Claude's does not. The colour lives in the adapter because it is one terminal's alphabet; the kernel states the sites in portable words and both runtimes carry the same verdict. |
| Post-write observation of an edited file | `PostToolUse` names the file, so the dead-suppression sweep (`repair_command`) and the type checker (`diagnostics_command`) both run on it, and the shell tool is observed beside the editing ones so what a command wrote is read after it ran | `PostToolUse` hands over the working directory alone, so neither runs on an edit; a shell command's writes are still reviewed, since a command carries its own text | The same fact one step coarser, and the same reason diagnostics were already Claude-only: Codex names its files inside the patch envelope, and this event runs after the patch has rewritten the file the decoder would validate against. Sweeping the directory instead would rewrite files the edit never touched — so on Codex a directive that silences nothing waits for `dev check --antipatterns --fix`, which is where every other whole-tree finding waits too. Both runtimes observe the shell tool, and that was not always so: the review of what a command wrote was wired into both dispatchers and reachable from neither, because the observed set named only the editing tools — which carry no command — so the gates a shell write cannot meet before it runs were met by nothing after it either. |
| Attributing a change to the session that made it | `Edit` and `Write` check exact paths against live peer claims before writing. Shell calls use before/after snapshots | Decoded patches check each source and destination against the same peer-claim policy before writing. Ordinary shell calls use the shared snapshot comparison | Peer claims are checked from decoded paths on both runtimes. Codex routes a contested patch through its review queue, including the holder's name in the reason. Post-tool patch decoding cannot reconstruct the preimage, so post-tool observation remains coarser. |

## Parity audit of generated artifact families

Every family in `.claude/` vs `.codex/`/`.agents/`, with an explicit decision.

| Family | Claude | Codex | Decision |
| --- | --- | --- | --- |
| Skills ({{ len_skills }}) | `commands/*.md` | `skills/*/SKILL.md` | Parity — same {{ len_skills }} declarations, native formats. |
| Agents ({{ len_agents }}) | `plugins/lup/agents/*.md` | `.codex/agents/*.toml` | Parity — same {{ len_agents }} declarations, native formats. |
| Plugin manifest | `.claude-plugin/plugin.json` + marketplace | `.codex-plugin/plugin.json` + `.agents/plugins/marketplace.json` | Parity — native schemas. |
| Hooks (`hooks.json`, `scripts/policy.py`, `runtime/kernel.py`, `runtime/policy_data.py`, `runtime/evidence.json`) | Structured decisions, edit inspection, autonomous identities | Structured native permission decisions, per-file patch inspection, explicit review-queue approval for exact retries, identities from the environment | Shared semantic kernel and edit gates; approval presentation differs as stated above. Pending native permission events are never approval evidence. |
| Guidance | `.claude/CLAUDE.md` | `AGENTS.md` + `.codex/config.toml` | Parity — one document, native locations. |
| Ownership proof | `.claude/.lup-ownership.json` | `.codex/.lup-ownership.json` | Parity — same mechanism per tree. |
| Template guidance | `TEMPLATE_CLAUDE.md` | `TEMPLATE_AGENTS.md` | Parity — shared portable sections, platform slices per flavor. |
