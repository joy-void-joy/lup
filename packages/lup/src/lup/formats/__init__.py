"""What a generated artifact is written as, at the leaf where data enters it.

Not what a document says, but what it has to be spelled like to survive being
one. A file compiled from a declaration has to say so, in whatever comment
syntax its own format admits; a value spliced into a compiled table has to
survive the characters that would end a cell or a row early. Both are one
question — the target format's rules, applied where data crosses into it — and
it is nobody else's: prose a human wrote is Markdown all the way down and
needs nothing here, which is why this sits below every package that compiles
something rather than inside the one that compiles most.
"""
