"""Per-engine clients: the ``Client``/``Session`` ABCs, the component verbs
(``Sessions``, ``Stream``), the composing ``ComposedClient``, the shared
machinery every composition draws on (``refusal``, ``usage``,
``responses``, ``display``), and each engine's components."""
