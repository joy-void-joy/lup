"""What an agent is given to act with, and what decides which of it it gets.

The tool decorator and server surface, the conditional availability policy, the
URL routing that sends a fetch to the tool that may serve it, and the
LSP-backed code intelligence the agent tools are built on. One subject: the
instruments, not the work done with them.
"""
