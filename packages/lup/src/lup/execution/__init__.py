"""Where work runs, and what it costs to run it there.

The container a session's commands are confined to, the retry and throttle a
transient failure is met with, the threads a synchronous call is handed to, and
whether a path can be written at all. One subject: not what is decided, but
what carrying it out runs into.
"""
