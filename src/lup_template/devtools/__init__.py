"""Development and analysis CLI tools for lup.

Provides the ``lup-devtools`` entry point. Which command groups it serves
follows from the modules this project adopted, so the list is the generated
``docs/commands.md`` rather than one written here that a declined module
would leave naming a command nobody serves.

Examples::

    $ uv run lup-devtools --help
    $ uv run lup-devtools agent inspect
    $ uv run lup-devtools trace list
"""
