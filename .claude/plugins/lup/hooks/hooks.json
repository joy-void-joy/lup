{
  "description": "Lup semantic permission policy",
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "python3 \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/policy.py\" || { status=$?; [ \"$status\" -eq 2 ] || echo \"lup policy: dispatcher exited $status without judging this call; refusing rather than passing it\" >&2; exit 2; }",
            "timeout": 30,
            "type": "command"
          }
        ],
        "matcher": "Edit|Write|Bash"
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "python3 \"$CLAUDE_PLUGIN_ROOT/hooks/scripts/policy.py\" || { status=$?; [ \"$status\" -eq 2 ] || echo \"lup policy: dispatcher exited $status without judging this call; refusing rather than passing it\" >&2; exit 2; }",
            "timeout": 30,
            "type": "command"
          }
        ],
        "matcher": "Bash|WebFetch|Edit|Write|Artifact|Skill|EnterWorktree"
      }
    ]
  }
}
