repos:
  - repo: local
    hooks:
      - id: lup-harness-generate
        name: regenerate Lup native harness artifacts
        entry: uv run lup-devtools harness generate all
        language: system
        pass_filenames: false
        always_run: true

// lup: I don't feel very confident in the current CI+pre-commit workflow. Can you walk me through it? Maybe we should pause it. What do you think there?
