<!-- Generated from lup.harness.content.docs.platform_differentiation by `uv run lup-devtools harness generate all` — edit the source, not this file. See docs/harness.md. -->

# Platform differentiation and parity

One portable declaration, two native renderings. `portable_harness()` in
`src/lup_template/harness/catalog.py` is deliberately singular: the
settled architecture is **portable-declaration-plus-adapter-rendering**, not
per-platform declarations with a shared default. Everything a platform does
differently lives in exactly two places — the adapter renderers
(`packages/lup/src/lup/providers/claude/harness.py`,
`packages/lup/src/lup/providers/codex/harness.py`, composed by
`packages/lup/src/lup/providers/harness.py`) and the per-platform generation
recipes (`claude_generation_recipe` / `codex_generation_recipe` in
`packages/lup/src/lup/devtools/harness/generate.py`). A per-platform declaration
layer was considered and rejected: it would let semantic content fork silently,
whereas the adapter seam forces every difference to be a rendering decision
over the same declarations. `compile_claude` / `compile_codex` enforce that:
`reject_rendered_invocations` refuses native invocation sigils in canonical
text, and `reject_native_prose` refuses any word an adapter would have spelled
— so a difference cannot hide in prose.

That second check writes down no vocabulary of its own. It asks each
`NativeSpellings` what it spells and forbids exactly that
(`packages/lup/src/lup/harness/codescan/portable.py`, rule `portable-content`), which
is why adding a location to `TreeLocation` or `PluginLocation` forbids it in
prose the same moment a runtime learns to spell it.

This document is the map of every intended difference and the parity audit of
every generated artifact family. "Parity" means the same semantic content in
each platform's native format — never byte parity.

Generated files are one surface, not the boundary of the audit. Launch readiness, authentication, host bridges, delegated-agent paths, runtime diagnostics, and verification must provide equivalent user-visible semantics too; a runtime-specific substitute belongs in this map with the evidence that proves its difference.

## Where each intended difference lives

| Concern | Claude | Codex | Why it differs (all deliberate) |
| --- | --- | --- | --- |
| Skill invocation spelling | `/lup:<skill>` (`ClaudeSpellings.render`) | `$lup:<skill>` (`CodexSpellings.render`) | Native sigils. Canonical content stores `SkillInvocation` parts; only the vocabularies spell them. `SkillPattern` carries the placeholder or wildcard form a prompt uses when it teaches the shape of an invocation instead of issuing one. |
| Prompt compilation | `ClaudeSpellings`: `$ARGUMENTS` for `ArgumentsRef`, the structured-question tool for `AskUser`, a delegation call for `Delegate` | `CodexSpellings`: prose arguments reference, a direct instruction to ask, a custom-agent delegation | One neutral `SpelledPromptRenderer` walks the parts; each runtime supplies a `NativeSpellings` for every native word. A new part adds an abstract method neither runtime can be constructed without answering. |
| Harness locations in prose | `.claude/CLAUDE.md`, `.claude/settings.json`, `.claude/plugins/lup/commands/`, … | `AGENTS.md`, `.codex/config.toml`, `.codex/plugins/lup/skills/`, … | `NativePath` and `PluginPath` name a location semantically. `scope="this_tree"` resolves to the reader's own tree; `scope="every_tree"` renders every runtime's spelling in one identical string, which is how prose teaches both at once. |
| Model choice | `model: opus \| sonnet \| haiku \| inherit` in agent frontmatter | row omitted | Agent declarations carry a portable `ModelTier`. Recorded evidence for Codex custom agents covers TOML parsing only, so no alias is proven to spell a tier in; omitting the row inherits the session model. |
| Runtime documentation | Claude Code and Agent SDK origins | Codex origins | `RuntimeDocs` points a reader at its own runtime's docs; both origins are already in the fetch allowlist (`harness/catalog.py`). |
| Session native tools | `native_tools=None` produces an empty built-in roster; exact names and `NativeToolGroup` values select Claude facilities | `native_tools=None` suppresses built-in inventories and inherited integrations; supported groups are `SHELL`, `WEB`, `WRITE`, and broad `ALL` | Every constructor and session config defaults to no native authority. Codex has no independently enforceable `READ` facility, so it rejects that group and exact `Read`, `Write`, and `WebFetch` names rather than widening them to shell, patch, or search authority. Exact supported Codex names are `Bash`, `WebSearch`, and `apply_patch`. Both adapters reject settings and provider overrides that could widen the grant. Explicit session hooks remain attached; Codex also enables the verified declared project policy plugin for a native grant. Its selected model must exist in the native catalog so tool metadata can be bounded. |
| Application tools with no native grant | Explicit `@lup_tool` handlers are hosted through MCP | Explicit handlers use app-server dynamic tools and dispatch in-process | The factory's `tools=[...]` and explicit `tool_servers` declarations remain independent of `native_tools`. Typed submission remains available. Codex resume must retain the same tool and submission schemas because native resume cannot replace its dynamic binding; incompatible resumes fail before input. Captured native fixtures inspect complete request inventories and tool execution, including metadata outside the top-level tool list. |
| Escaping the sandbox for one call | a per-call flag on the launching tool | a per-command override on the shell call, with a justification the approval policy records | `NativeSpellings.escape_sandbox` returns a `Spelling`, so a runtime with no words an agent could be told to use answers `Unsupported` rather than naming a flag its reader cannot pass — and one that has them says so instead of inheriting a gap nobody re-checked. Both resolver entries ask it, so neither can hardcode an escape and neither can be silently absent. |
| Placing a call from the hook's own verdict | the verdict rewrites the call's arguments | no channel: the verdict is an accept or a decline | `NativeSemantics.escapable` is the whole of the difference — whether this runtime's verdicts can place a call. Where they cannot, the plain effect is rendered rather than an intent the runtime would drop. Asking for the launcher's host is not this question: it is `# lup: escalate[sandbox]`, one spelling under both runtimes, answered by Lup's reviewer rather than a native flag. For a command the boundary declaration excludes, the model requests placement on its call and a compiled prefix rule approves exactly the excluded set. |
| Handing a whole document to a tool | the runtime's own file reader | declined, with the reason | `NativeSpellings.read_document` steers away from text extractors, which return an empty string on a scanned page and read as an empty document. Codex's roster reads a document only by running a command over it, and the one tool that takes a file whole accepts images alone. |
| The reason a verdict carries | `permissionDecisionReason` on the hook output, and every generated dispatcher renders it | the in-process approval reply carries only a native decision; the responder also delivers the reason to the active turn through steering and logs it; the generated dispatcher renders its native reason | The reason is the one channel to whoever reviews a stopped call, which is why every kernel reason names the specific subject that tripped it. Where the native approval response cannot carry text, Lup sends feedback separately to the matching active turn. Delivery failure refuses the call and leaves inbox receipts unacknowledged; it never turns missing feedback into permission. |
| The recovery a verdict carries | a queued question returns a denial joining reason and operator recovery commands; an allowed retry carries the settled verdict | a queued question and a refusal carry reason and recovery together: stderr with exit 2 at `PreToolUse`, or the native denial's `message` at `PermissionRequest`; the in-process responder delivers the pair through turn steering and logs it | `KernelDecision.recovery` is addressed to the agent and never to the approver, so a runtime splits the two wherever it has two channels and joins them wherever it has one. `KernelDecision.addressed` is that join, spelled once so no dispatcher composes its own. |
| The sentence an approved sandbox escalation adds to its question | The rendered request carries the placement the dispatcher measured, so the sentence a person answers says whether the call runs outside the host sandbox or with the per-call sandbox off inside the container's existing mounts. The allowed `Bash` verdict carries the complete input in `updatedInput`, with `dangerouslyDisableSandbox` set from the approved placement | Both `PreToolUse` and `PermissionRequest` asks require explicit review receipts, carrying the reason and recovery in their denial. Host-executor requests may reach review through the native permission event after pre-tool deferral. This dispatcher cannot rewrite placement; its final `placed(escapable=False)` composes no extra crossing sentence. One pre-tool receipt can advance once to the permission event only with the same nonempty native call ID, payload, session and file preimages | Approval does not change a container's mounts. `apply_sandbox_environment` arms the inner sandbox only for that launch posture; under an outer container the per-call flag lifts no mount restriction. Shared Git `config` and `hooks/` mounted read-only remain read-only, while human-owned files are governed by their review policy. Native approval mode and observed execution supply no Lup authority. The [native hook contract](https://learn.chatgpt.com/docs/hooks) does not promise `tool_use_id` on `PermissionRequest`: without that identity the pre-tool receipt cannot carry forward, and the call stays blocked pending fresh review or operator execution. `test_policy_settlement.py`, `test_claude_dispatcher.py`, `test_reason_voice.py`, `test_codex_review_queue.py`, and `test_native_approval_authority.py` cover placement wording, exact receipts, event handoff and replay. |
| Skills | `.claude/plugins/lup/commands/<name>.md` (description, allowed-tools, argument declarations) | `.codex/plugins/lup/skills/<name>/SKILL.md` (name + description frontmatter) | Claude plugin commands support tool restriction and argument frontmatter; Codex skills do not — arguments arrive as free text. |
| Agents | `.claude/plugins/lup/agents/<name>.md` (Markdown frontmatter) | `.codex/agents/<name>.toml` (custom-agent TOML) | Native agent formats and locations. |
| Plugin manifest + marketplace | `.claude/plugins/lup/.claude-plugin/plugin.json`, `.claude/plugins/.claude-plugin/marketplace.json` | `.codex/plugins/lup/.codex-plugin/plugin.json`, `.agents/plugins/marketplace.json` | Native manifest schemas and marketplace registries. |
| Plugin preparation at the execution boundary | The verified plugin directory is mounted into the container and passed with `--plugin-dir` | Interactive launches and contained worker wrappers install into the home the runtime actually opens, then verify native discovery and hook trust before authentication or session startup | A container's private home is prepared through the library's `lup-codex-plugin` executable. `harness codex-plugin install --codex-home PATH` exposes the same operation on the host. Application-opened host sessions resolve explicit environment overrides before inherited `CODEX_HOME` and the native default. Portable sessions capture the configured application project as `policy_root`, independently of a temporary native `cwd`; direct `CodexSessionConfig` callers may set `policy_root`, defaulting to `cwd` when omitted. Native discovery and hook trust are checked in the actual workspace and home before the session process starts; absent declared hooks fail closed. Owned homes may seed trust; personal homes retain their trust decisions. Projects without a declared plugin still open normally. |
| Repository guidance | `.claude/CLAUDE.md` | `AGENTS.md` at the repository root, plus `.codex/config.toml` (`[features] hooks`) | Same guidance document, rendered to each platform's documented location; Codex additionally needs the hooks feature flag. |
| Hook dispatch | Decodes `Bash\|WebFetch\|Edit\|Write\|SendMessage\|ListAgents\|Agent`; an ask is returned as a native permission request carrying the reason that earned it | Decodes `Bash\|web_fetch\|apply_patch\|collaborationspawn_agent`; native patches and standalone literal shell patches reach the per-file edit gates. Asks at both `PreToolUse` and `PermissionRequest` use the explicit review queue. Permission events return native `allow` or `deny`; only an explicit policy deferral leaves the native approval flow to decide | Both apply the same edit policy, and each puts its question through the channel it has: a request the session renders, or a receipt bound to session, payload and edited file preimages where there is no such channel. An autonomy mode answers a rendered request on the session's behalf, so what it settles is what that mode was turned on to settle; a receipt is the answer a mode cannot give. Post-tool execution never grants reusable authority: it completes a dispatched review or diagnoses unexpected execution as `in_doubt`. Generated-dispatcher tests cover both runtimes. |
| Autonomous edit identities | `policy_data.py` grants the resolver's `worker_identity`, bare and plugin-qualified, from the hook payload or the session environment | Same declared identities, from native `agent_type` or the session environment | Both lists are derived from `ResolveSpec.worker_identity`. Captured Codex 0.155.1 subagent tool calls in `tests/unit/fixtures/subagent_cleanup/payloads-codex.jsonl` carry `agent_type`; top-level worker sessions use the launcher's environment. Neither channel bypasses human-owned paths. |
| OS sandbox boundary | The `HookSandbox` declaration compiles into the `settings.json` `sandbox` block (bwrap network allowlist and credential read denials; a human-owned path is left to the policy, which asks); the launcher verifies `bwrap`/`socat` before exporting `LUP_SANDBOX_ACTIVE` | The launcher establishes an explicit `--sandbox workspace-write` envelope on the interactive command line and exports the flag only for an envelope it set itself; a caller-supplied sandbox flag keeps the deny lattice active | Codex sandbox config has no domain allowlist, so its envelope is the declaration's strict subset (network off); the resolver paths carry their own explicit envelopes on both platforms. |
| Forge credential | Selected once by `GitAccess.select` and carried into the container both runtimes launch through, identically: the same rung, the same rewrite direction, the same mounts, the same bare `-e NAME` | Same, from the same declaration | The credential is a property of the container, and one `Image` declaration starts every runtime — so there is nothing here for a runtime to spell differently. What does differ is only how far the *denial* beside it is enforced: `credential_paths` reaches Claude's native per-path credential sandbox and has no Codex equivalent, so on Codex the read denial is the semantic policy alone. Neither is a syscall boundary, and neither stops `ssh` or `git` using the identity that was lent — `docs/permissions.md` says so in those words rather than implying a stronger boundary. |
| Device lease | Granted per machine in `sync.json.local` (`sync grant`) or for one launch with `--device`, resolved against the host's CDI registry, and rendered as `--device` on the container both runtimes launch through | Same, from the same grant | A device is a property of the container and of the machine, never of a runtime or of committed code: one grant reaches every runtime and every resolver worker opened on that machine. The host postures (`--sandbox inner`, `--sandbox none`) run on the host, which holds its own devices, so neither runtime spells anything there; a `--device` under one is answered with a notice saying so. |
| Resolver entry | `/lup:resolve` instructs `uv run lup-devtools resolve --adapter claude` | `$lup:resolve` instructs the same command with `--adapter codex` | Both entries only launch the shared persisted Python resolver, and differ solely in the adapter they name — `ResolverEntry` is deliberately undifferentiated because workflow scripts execute in an isolated VM with no shell, leaving the Claude entry nothing to wrap. The entry contract is the CLI's: optional `--run-id <id>` (resume) and repeatable `--answer <question-id>=<value>`, through which the reserved `integration-assembly` gate is approved like any other question. Both rendered entries document it (pinned by `test_generated_resolver_entries_only_launch_the_shared_python_core`, which also asserts no `Workflow(` wrapper appears). |
| Downstream template guidance | `.claude/plugins/lup/TEMPLATE_CLAUDE.md` from `content/template_claude.py` | `.codex/plugins/lup/TEMPLATE_AGENTS.md` from `content/template_codex.py` | Both flavors compose the portable sections in `content/template_sections.py`; only platform slices (guidance-file names, meta-agent naming, native edit and approval guidance, LSP vs CLI diagnostics, settings, communication idiom) differ. |
| Launch and trust | Launches the verified local plugin directory with `--plugin-dir`; `CLAUDE_CONFIG_DIR` selects the profile | Seeds a persistent per-worktree home from personal authentication and settings, materializes the Claude daltonized theme without selecting it, and publishes a verified content-addressed plugin revision; explicit `--codex-home`/`CODEX_HOME` overrides bypass isolation | Codex installs in a temporary home, verifies the native output, then publishes the revision and only its marketplace/plugin registration. Native installation never runs against the live home: its cache pruning would remove earlier revisions. Concurrent Lup publishers serialize, unrelated settings survive, and existing revision paths remain available. |
| A launched session's coordination identity | The durable id is exported as `LUP_COORDINATION_MEMBER`, and `--name <worktree>` makes the runtime's own display agree with the roster. A session nobody launched joins under the id Claude Code gave the process instead: its stdio tool servers find it as `CLAUDE_CODE_SESSION_ID` in their environment and its hooks read the same value on stdin as `session_id`, so the server that beats and the hook that departs name one row | The same durable variable, which reaches the tool servers only because each names it in `env_vars`: Codex starts a stdio server under a fixed base environment. Root `SessionStart` and `UserPromptSubmit` hooks bind the native thread, configuration home and execution scope to an existing matching launcher member; child events cannot overwrite it. A session nobody launched joins under nothing: Codex documents no session id reaching a server it starts, so its tool server serves no coordination verbs, and its hooks fold the record and beat without ever joining a row | Both halves are minted in `session_argv`, the one place either launcher passes through, so the id is a fact about having been launched rather than about which CLI was. The unlaunched fallback is each adapter's own answer, asked through `lup.providers.identity` by runtime name, so the variable's spelling stays where the runtime that sets it is spelled. On Claude Code it is measured rather than documented — read off a running coordination server under 2.1.272, while the env-vars and hooks pages document `CLAUDE_PROJECT_DIR` for servers and neither session variable — and a release that stopped setting it would cost an unlaunched session its identity and a launched one nothing. On Codex the absence is documented: hooks receive `session_id` (https://learn.chatgpt.com/docs/hooks) and a server's `env`/`env_vars` name variables to set or forward (https://learn.chatgpt.com/docs/extend/mcp), none carrying the session's id. What a launcher exported reaches only a server that asks for it: measured on Codex 0.155.1, a stdio server started with `LUP_COORDINATION_MEMBER`, `LUP_COORDINATION_NAME` and `LUP_MAX_RECURSIVE_AGENT` in Codex's own environment sees `HOME`, `LANG`, `LC_ALL`, `LOGNAME`, `PATH`, `SHELL`, `TERM`, `TMPDIR` and `USER` and none of the three, and all three once its `env_vars` names them — which every tool server here does, from `lup.harness.environment.tool_server_env`. Measured against Codex 0.153.4, whose launch options are `-c/--config`, `--enable`, `--disable`, `--remote`, `--remote-auth-token-env`, `--strict-config`, `-i/--image`, `-m/--model`, `--oss`, `--local-provider`, `-p/--profile`, `-s/--sandbox`, `--approve-for-me`, the two `--dangerously-` forms, `--add-dir`, `-a/--ask-for-approval`, `--search` and `--no-alt-screen` — none of which names a session. The difference costs a peer nothing: what addressing resolves through is the names on each member's own file in lup's store, so the roster answers to the same derived worktree name on both runtimes and `dev coordination rename` changes it on both. Only the CLI's own chrome differs, and only on the runtime that has chrome to put it in. |
| Mail reaching a peer mid-session | An unconditional `PreToolUse` delivery group, separate from permission decisions | The same unconditional delivery group and shared reader, including tools outside the policy matcher | Both generated guards emit the documented `hookSpecificOutput.additionalContext` envelope. Neutral mail grants no permission; redirects carry their full text in context as well as the denial reason. Codex 0.155.1 collects context even when another hook denies the call. Mail is consumed only after stdout flush succeeds; malformed input, child events and failed writes leave it pending. Tests run both rendered guards against the typed store, including failed output. Pipe acceptance is verified; model consumption has no hook receipt and was not measured live. |
| The roster reaching a session at prompt time | A `UserPromptSubmit` group carrying a shell guard that exits where this repository has no roster and otherwise hands over to the `coordination_changes` entry beside the shipped `coordination` package, whose `changes` fold's `hookSpecificOutput.additionalContext` is what changed since this session's last prompt: facts newly standing over the repository first, then contested paths under this checkout, then paths a peer holds here, arrivals, departures and redescriptions, with whatever is standing and a one-line pointer to `coordination_peers` on the first prompt and nothing at all when nothing moved | The same guard and the same fold under the same event name, `UserPromptSubmit`, which Codex documents beside `SessionStart`, `SessionEnd`, `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PreCompact`, `PostCompact`, `SubagentStart`, `SubagentStop`, `Stop` and `Interrupt` | One rendering rather than two, because the adapters own two words — the event's name and the variable the plugin root is exported as — and the fold lives in `lup.coordination.bare.changes`, over the one store fold beside it, rendered by `lup.providers.roster_prompt` wherever a `peer_policy` is declared. Both vendors document the same input (`session_id`, `cwd`, `prompt`, `hook_event_name`) and the same output shape read from stdout on exit 0: Claude Code at https://code.claude.com/docs/en/hooks, Codex at https://learn.chatgpt.com/docs/hooks, where https://developers.openai.com/codex/hooks redirects, adding that `matcher` is not read for this event and that `additionalContext` is capped near 2,500 tokens per hook by default, past which it spills to disk — a fold of a few lines never nears it. The evidence differs by runtime and is stated as such: on Claude Code the rendered guard's stdout is measured by a test that runs it over a store the typed writers produced, and its delivery in a live session is not yet measured; Codex root arrival and rendered context artifacts have deterministic tests, while authenticated model consumption has not been measured in this check. What *is* verified on both is the artifact — the two generated guards differ only in the variable their plugin root is exported as, resolve the same store root from `git rev-parse --git-common-dir`, hand over the same `LUP_COORDINATION_MEMBER`, and carry byte-identical copies of the shipped `coordination` package. What a session hears *between* prompts is unchanged by this row: mail arrives through an unconditional delivery hook on both native plugins. `coordination watch --as-run` attempts Codex queue delivery only through a matching recorded home and execution scope. An owned stdio receiver can also queue its pending mail locally across container boundaries; the native idle-wake integration measures a second turn on CLI 0.156.1 through the recipient's default Unix control socket — a wake on top of the record, never the roster's changes, and the watcher's business rather than this hook's. |
| A session's presence, and its departure | Two signals. The coordination tool server beats for the session it serves — one file per member under the store, touched every 30 seconds for as long as the server runs, each tick sweeping the rows whose pulse stopped and joining its own again — so a session that was killed or whose container stopped reads as gone within two minutes, and its held paths and its redirectable address go with it. A `SessionEnd` group carrying the same shell guard as the prompt hook hands over to the `coordination_departure` entry, which appends the session's finish through the same store fold every reader folds it with, so a clean exit, a cleared conversation and a termination signal end the row at once; the prompt hook beats too, at each prompt | The same server, the same beat, and the same `SessionEnd` guard and writer, under the event name Codex documents beside `SessionStart` | One rendering per signal, in `lup.coordination.bare.store`, registered by `lup.providers.roster_prompt` wherever a `peer_policy` is declared. Presence is read three ways and the rule is one *because it is one function*: the typed listing, the prompt-time fold and the compiled permission dispatcher all call that module's `present`, which reads a session as here only while its newest record or its latest beat is within the window that module declares — so a rule the three would otherwise keep separately cannot be kept differently. The design leans on no ending being exact: a finish the process outlives is undone by the server's next tick, which joins again, and a real exit stops the ticks, so that finish stands. Claude Code documents `SessionEnd` at https://code.claude.com/docs/en/hooks-guide with `session_id`, `cwd` and a `reason` among `clear`, `resume` (the session saved for resumption), `logout`, `prompt_input_exit` and `other`, and documents that `/clear` fires the session events while MCP servers stay connected — which is the case the re-join covers; what a checkpoint rewind or an interrupted turn fires is not documented, and nothing here depends on it. Nothing fires on a kill the process cannot catch, which is what the pulse is for. On Codex the event rests on its documentation alone, as the prompt hook's does. |
| A subagent's background work at its report | A `SubagentStart` group and a `SubagentStop` group, each carrying a shell guard that hands over to the `subagent_cleanup` host half beside the shipped kernel. At the start the fold's `hookSpecificOutput.additionalContext` is one sentence: what the subagent arms in the background is its own to stop with `TaskStop` before it reports. At the stop the fold reads the payload's `background_tasks`, which is the whole session's, and the subagent's own transcript at `agent_transcript_path`; a listed task is the subagent's when its command is one a `Monitor` or backgrounded `Bash` call there armed, or its description and type are what an `Agent` call there started, and the subagent's own entry is never its to stop. While any such task is listed the report is refused once, as a block decision whose reason names each task's id and the call that ends it; the stop that follows carries `stop_hook_active` and goes through | A `SubagentStart` group alone, carrying the same shell guard over its own `subagent_cleanup` host half beside the shipped kernel. The fold's `hookSpecificOutput.additionalContext` is one sentence in Codex's own vocabulary: what a subagent opens with `exec_command` is its own to stop with `write_stdin` carrying the interrupt before it reports, and work left running outlives that report and holds what it opened until the session ends. No `SubagentStop` group, and that absence is a measurement rather than a gap. Both halves were measured on 0.155.1 in operator-run sessions and they come apart: `sleep 494`, opened through a subagent's PTY with no `&`, was alive 28.2 seconds after `SubagentStop`, its `etimes` matching the elapsed time exactly, so the same process and not a reparented orphan; and a `tail -f` on an empty file armed the same way, with a line appended 14.4 seconds after the stop to force output onto the surviving session, was followed by twelve records of which **none** carried the subagent's `agent_id` — no second stop, no tool call. So the work outlives the report and its output wakes nobody, and a block here would refuse a report to prevent a loop this runtime does not have; the sentence is owed and the refusal is not. Codex hands `agent_transcript_path` with no task list, which is what the stop-time fold would have read had there been one to register. #494 carries both runs | The judgement is `lup.policy.kernel.subagents`, compiled into both trees; the events, the payload's keys, the transcript's shape and the tool names are each host half's — `lup.providers.claude.assets.subagent_cleanup` and `lup.providers.codex.assets.subagent_cleanup` — registered by `lup.providers.subagent_cleanup` wherever the hook set declares `subagent_cleanup`, which it does by default. A host half naming no stop event takes the sentence alone. Measured on Claude Code 2.1.278 and kept as fixtures under `tests/unit/fixtures/subagent_cleanup/`: a background subagent's monitor survives its report and each line it emits resumes the finished subagent, which the runtime's own notification describes as "may resume on its own when that work completes or reports"; the stop-time list types a monitor and a backgrounded command alike as `shell`, holds the subagent's own entry, and marks nothing as anybody's; a block on the first stop made the subagent `TaskStop` its monitor within seconds, and the second stop carried the flag and an emptied list. In auto mode, where the report is a `SubagentHandback` call, the subagent's first stop still precedes that call, so the block lands before the report and exactly one report follows the cleanup. The main agent is deliberately not gated: its `Stop` fires with background subagents listed as running, and that wait is wanted. On Codex, measured on 0.155.1 in user-run sessions: the parent awaits a subagent through `collaborationwait_agent` rather than backgrounding it, the subagent's tool calls carry `agent_id` and `agent_type` although the docs say nothing of it, its shell call was handed a session id, and that session outlived the report while waking nobody. Which of the two a runtime has is what its host half declares in `Leftover`, and the notice is composed from it, so a subagent is never promised a refusal that runtime does not perform — a promise that never comes true is what teaches it to discount the next one. |
| A native call that would reach a peer | `SendMessage` and `ListAgents` are routed and decided. A send whose input names a live member of this repository's roster is denied, with the durable surface named rather than only refused; every other target passes through untouched, which is what leaves subagent continuation and every session this repository does not hold working exactly as before. A listing decides nothing and carries the roster in `additionalContext` beside it | Nothing extra: no call in its roster addresses another session, so there is none to route | One runtime's tool roster differs, not the judgement — which is settled once in `lup.policy.kernel.peers`, compiled into both trees, and reached through host-folded spellings because the kernel resolves nothing itself. Every string the call carries is matched rather than a named field, for the reason a refusal is: which key spells a recipient is the runtime's own business, and a check knowing one spelling fails open on the rest. The listing is deliberately not refused. Measured against a live account, most of what it returns is sessions in *other* repositories, which a repository-scoped roster is structurally incapable of holding — so a redirect would answer a question the reader did not ask, and would be escalated past every time. Attaching rather than deciding is why the verdict is `defer`: lup grants nothing it was not asked to grant, and the runtime's own permissions still settle a call it says nothing about. `additionalContext` on `PreToolUse` is a documented field whose delivery moment the vendor leaves unstated, so the attachment fails open — a roster that never arrived would cost a reader one attachment and nothing else. Measured in a live session on 9 September 2026: the hook's context reached the model **in the same turn as the tool result**, labelled as the hook's additional context beside the listing, and was readable before the model acted on either. The listing that came with it named twenty sessions, every one in another repository and offline, which is the population a repository roster cannot hold and the reason the call is attached to rather than refused. |
| A spawn without a name, or with one no runtime here would take | `Agent` is routed and decided: a call whose input carries no `name`, or a blank one, is denied with the reason and the recovery the hook set's `spawn_names` declares — the shape a name takes and what it is for — and so is a name outside that shape, named in the refusal beside it. A call carrying one of the right shape is deferred to the runtime's own permissions. An escalation marker in any of the call's strings turns either denial into the approval question | `collaborationspawn_agent` is routed and decided the same way over `task_name`, the field its spawn carries. The spelling half is what fires here: a hyphen this runtime rejects is refused before the call rather than after it | One judgement, `lup.policy.kernel.spawns`, compiled into both trees; which tool spawns and which field names the spawn are each host half's. Measured on Claude Code 2.1.278: a subagent is listed, addressed and stopped by the `name` its spawn carried, and by its type where none was, so a session with several general-purpose subagents shows several rows saying the same thing; the tool accepts `name` although the schema shown to the model omits the parameter, which is why a model passes one only when told to, and the recovery tells it. Its own validator, read out of the shipped binary, says "name must start with a letter or digit and contain only letters, digits, underscores, or hyphens (max 64 chars)". Measured on Codex 0.155.1 in a user-run session: the spawn carried `task_name`, the hook named the tool `collaborationspawn_agent`, and a hyphenated `pty-arming-probe` produced **no `PostToolUse` at all** — the model retried as `pty_arming_probe` unprompted, having learned the shape by guessing. That is why the spelling is judged here and not left to the runtime: one of them refuses where nothing records it. The declaration takes the intersection of the two — a letter or digit first, then letters, digits and underscores, at most 64 — and a project running on one runtime alone may widen it. The response is a path-like handle rather than the name passed in — a spawn named `long_lived_session` came back carrying `/root/long_lived_session` — so what a caller passes is not what it gets, and nothing downstream should match a subagent by that return. Deferred rather than allowed on both, because the kernel grants nothing it was not asked to grant. |
| Waking an idle peer | `wake()` writes one newline-delimited JSON frame (`{"type":"user","session_id":"<the member's>","message":{...}}`) to the session's own inbox socket, whose path the launcher placed and the member declares; the session takes a turn on it | `CODEX_HOME=<target home> codex queue --thread <uuid> --message <text>`, only when the recorded execution scope matches the sender; zero exit means queue acceptance | Codex selects its app-server through the configuration home. Lup records that home with boot, namespace, process-root and effective-user evidence, and refuses missing or mismatching routes. Owned stdio coordination servers run an `InboxRelay` inside the receiving boundary. It rereads the hook-bound route, queues fresh durable message IDs locally, and records native acceptance without consuming mail. A shared lock serializes receiver instances; failures retry, route changes permit redelivery, and shutdown waits for the bounded queue attempt. In-process registrations have no companion lifecycle and no relay. This is at-least-once notification: a crash after native acceptance, a direct sender or an external watcher can duplicate a nudge, and a hook can read the mail before the nudge arrives. Deterministic tests prove these transitions with fake native queues. The inert local integration on CLI 0.156.1 also proves browser decision → durable mail → real recipient relay → native queue → a second started and completed turn containing the operator's nonce, after the initial turn completed. It uses the recorded home and normal Unix control socket discovery without manual resume or external account credentials. A wake is always *on top of* the mail, never instead of it, so the record is identical and only the latency differs. Measured on Claude Code 2.1.278 rather than inferred: a background session confirmed idle by `claude agents --json` (`status: idle`) was sent a frame by a plain script and answered a token it could only have read there; the same again through an inbox relocated by `--messaging-socket-path`. The authentication frame the runtime's own startup text calls required is **not** needed — a frame sent without one was accepted — so reachability of the socket is the whole of the credential, and the directory those sockets live in is the boundary. **So lup places them** (`lup.harness.messaging`): the launcher passes `--messaging-socket-path <dir>/<member>.sock` for every session it starts, and one bind mount carries that directory into each container *under the same path it has outside*, because the path is a datum a member publishes and another container reads back. Named after the member rather than its process: two contained sessions are each pid 7 in their own namespace, so the runtime's own `/tmp/cc-socks/<pid>.sock` is one path with two owners. The directory is deliberately **not** one the runtime scans — it matches none of the four anchored patterns its binary carries (`/tmp/cc-socks`, the `/private/tmp` and Termux spellings, `/run/user/<uid>/cc-socks`) — measured, by launching a session into it and finding the runtime's own directory still holding only the launcher's socket. Sharing the scanned directory instead would make every session on the machine natively reachable by every other through a file channel `lup.policy.kernel.peers` cannot see, since that guards tool calls. The frame carries the recipient's `session_id`, which the receiving inbox checks against its own and drops on a mismatch — measured, by writing two frames to one live session's socket and seeing only the one naming it arrive — so a nudge that reached the wrong process is refused by it rather than delivered. Claude's CLI has eighteen verbs at this version and none speaks to a live session; the path is the socket, not a verb. Discovery is separately two paths — a scan of the socket directory, and a registry under `$CLAUDE_CONFIG_DIR/sessions/` carrying `messagingSocketPath`, `name` and `status`, which **only `kind:"bg"` sessions publish to** — so an interactive peer is invisible to a native listing while still being woken by path; measured again from the interactive side, where a listing reported neither of two live probe inboxes sitting beside its own socket. A member that declared no path is told so rather than silently un-nudged. The tested Codex native path starts an idle turn; `wake()` still reports `reached` on command acceptance, which alone cannot confirm that a particular recipient has read the notification. The Windows named-pipe transport, and the uncontained and docker postures, are likewise unmeasured. |
| Runtime preflight | `claude` CLI version, plugin support, `plugin validate` | CLI version, cache digest, and native `account/read` with managed-token refresh | Account checks use the session's execution boundary: the selected host home for inner/none, the container's home for outer. Sign-in uses the browser on the host or in a container sharing host loopback, and device authentication where the container has a separate loopback interface; both are checked again afterward. The browser bridge hands the sign-in URL to the host, and the printed URL can be opened manually when automatic opening is unavailable. Contained profiles are materialized as the actual base settings, so the account check sees their selected provider. Host named profiles remain explicitly unverified because the account API cannot select them. Declining sign-in continues explicitly unverified; account readiness does not prove implicit MCP service startup. |
| Selected personal settings | The selected Claude home supplies the runtime configuration | A contained launch transports the selected base and optional named-profile layer over stdin, validates it with native `config/read`, and materializes it as `/cfg/config.toml` in persistent state scoped by the settings digest. Host named profiles use immutable local profile files | Destination plugin registration, project trust, and hook trust remain local; declared Lup hooks stay enabled. Files are published atomically with mode `0600`; diagnostics omit setting values. Relative configuration paths retain their source-home origin, and referenced files must be accessible through the launch mounts. A previously copied scoped-home configuration has no original path provenance: use absolute accessible paths when its relative references no longer resolve. Changed settings select separate persistent native state; prior volumes and their logins remain available, and the existing selected-host credential handoff seeds the new state. |
| Named profiles in application-opened sessions | The selected home is the session configuration | `CodexSessionOpener` and profile transforms refuse `named_profile` with `UnsupportedCapability` before startup | Codex 0.155.1 rejects `app-server --profile`. A native no-turn probe confirms that `thread/start.config` can set model/provider/effort while `account/read` still uses startup authentication. Native configuration also declares `model_catalog_json` thread overrides to be no-ops and some service settings to require restart (https://github.com/openai/codex/blob/main/codex-rs/config/src/config_toml.rs). Configure the intended existing `CODEX_HOME` before opening an SDK session, or supply its supported explicit per-session settings. Interactive launchers materialize the selected settings at the native boundary (https://learn.chatgpt.com/docs/config-file/config-advanced). |
| Reasoning effort | `CLAUDE_EFFORT` in `providers/claude/selection.py` | `CODEX_EFFORT` in `providers/codex/selection.py` | `SessionRequest.effort` is asked for in portable words and mapped by each adapter, the way autonomy already is. `low`, `medium`, `high`, and `xhigh` are the four rungs both ladders carry outright. The two ends belong to one runtime each and the other renders the nearest it has: `minimal` is Codex's own floor, which Claude meets with `low` because its ladder has no rung beneath that; `max` is Claude's own ceiling, which Codex meets with `xhigh` for the same reason at the top. Codex's `none` is deliberately absent from the portable vocabulary — Claude would render it as `low`, turning "do not reason" into "reason a little" on one runtime without saying so. |
| Typed turn output | Lup's submission tool validates and returns gate feedback during a native turn | Native per-turn strict `outputSchema`, followed by the same Pydantic validation and submission gate; schemas with optional fields, defaults, arbitrary mappings or other constructs outside the strict subset travel as an `output_json` string containing the original JSON value | The carrier preserves the original schema's values and validation semantics. Rejected JSON and gate feedback use the shared bounded correction middleware. Schema changes, untyped turns and resumed threads retain conversation identity. `CodexSessionConfig.correction` controls correction attempts; events, usage and active steering cover all attempts. |
| Exact request limits | Native `max_turns` and `max_thinking_tokens` | Both raise `UnsupportedCapability` before starting a session | Codex 0.155.1 exposes no equivalent native limit. Use `effort` or whole-turn client timeout/budget middleware only where their different semantics fit. |
| Completed native activity | SDK content blocks | All app-server item kinds retain replay evidence; native tools carry call/result blocks, and plans, compaction and future kinds carry `TurnNativeActivityBlock` | Provider activity survives typed serialization and telemetry without being mislabeled as assistant speech. |
| Portable session hooks | Native SDK hook callbacks | `PostToolUse`, bounded `Stop` continuation, inbox context, and callbacks scoped exactly to native command/file-change approval requests | Codex app-server exposes approval requests, not interception of every tool. Both direct and portable openers refuse broad pre-execution callbacks before session startup; explicitly scoped approval callbacks also require a native requesting policy. Callback feedback reaches the matching active turn through steering, or a bounded continuation after completion; delivery receipts are acknowledged only after transport acceptance. Stop evaluation precedes typed submission and retains its continuation flag through validation retries. Generated CLI hooks remain a separate native plugin surface. |
| A session's containment | `CLAUDE_CONTAINMENT` in `providers/claude/selection.py`, rendering the SDK's `sandbox` settings, and `cli_path` for the program that enters the container | `CODEX_CONTAINMENT` in `providers/codex/selection.py`, rendering the one `sandbox` mode, and `executable` for that same program | `SessionRequest.containment` asks in the launcher's own three words — `outer`, `inner`, `none` — so one request means one wall on both runtimes, and the default is the `none` every request meant before the axis existed. What differs is what each runtime has to spell it with. Claude holds a permission mode and a sandbox, so autonomy and containment render into two fields that decide nothing about each other. Codex has one field for both — it says how much a session may do by saying how far it may reach — so `codex_sandbox` takes the narrower of what the wall asks and what the autonomy implies, over `read-only < workspace-write < danger-full-access`: neither axis may widen what the other narrowed, which is a refusal rather than a precedence rule. `outer` is the exception under both, and takes the field outright. The runtime's own sandbox is stated off rather than left unsaid, because a spawned session reads none of the settings files a launched one does and the policy would otherwise judge a posture nobody set; on Codex the boundary is a kernel facility an unprivileged container will not nest, so narrowing it there would arm the wall that cannot start. The program both fields point at is written by `contained_cli` in `devtools/harness/contained.py` — one builder, whose only provider-specific argument is the CLI's own name. |
| Usage display | the OAuth usage endpoint for live windows, plus the local stats cache for per-day and per-model detail | the app-server's own account calls for both the metered windows and the daily token buckets | One display over two readers (`lup.observability.usage`, `usage/reader.py` in each adapter). Each side reports a plan's windows and its days into the same report, so the pacing bars, the daily budget, and the `--json` snapshot are decided once. What differs is what each account publishes: fixed named windows and a per-model split on one side, two self-describing windows and no model breakdown on the other — which is why one draws a model legend and the other has none to draw. |
| Sensitive local-only files | `.claude/settings.local.json` | `.codex/config.local.toml` | Native personal-config locations, excluded from generation. |
| Where a verdict's question is put | The native permission request, carrying `permissionDecisionReason`; a verdict enumerating sites repeats them in `systemMessage`, coloured for this terminal (`announced` in `providers/claude/assets/policy_dispatcher.py`) | The review queue, because the pre-tool boundary has no ask effect: a successful structured `deny` carries the reason in `systemMessage`, and app-server hook notifications deliver it as a warning. Refusals from anywhere else write their reason to stderr | Measured against Claude Code 2.1.237 and Codex 0.155.1. Claude's `permissionDecisionReason` reaches the person for `Bash` and not for `Write`/`Edit`; `systemMessage` reaches them from every hook, arriving with the tool call rather than with the prompt. Codex drops `systemMessage` on exit 2, so a blocked review exits 0 to preserve the warning while the structured denial still stops the call. `codex exec --json` omits hook notifications, so its agent receives the denial but its event stream carries no separate review notice. An autonomy mode answers a rendered request on the session's behalf, and the payload carries no field separating that from a person; a recorded answer is the one a mode cannot give. |
| Post-write observation of an edited file | `PostToolUse` names the file for the dead-suppression sweep (`repair_command`) and type checker (`diagnostics_command`); shell writes are reviewed after execution | `PreToolUse` records hashes for the patch's paths under its native call id; `PostToolUse` compares actual bytes before claiming, repairing and checking changed paths. Standalone literal shell patches follow the same path | Both runtimes record file claims and repair before checking. Updates, additions, deletions, and both sides of moves are identified without requiring removed files or old context. Failed patches leave unchanged files alone; partial failures check only actual changes. A missing matching snapshot diagnoses the missing evidence without mutating files. Ordinary shell writes retain their shared post-execution review. |
| Attributing a change to the session that made it | `Edit` and `Write` check exact paths against live peer claims before writing. Shell calls use before/after snapshots | Decoded patches check each source and destination against the same peer-claim policy before writing. Ordinary shell calls use the shared snapshot comparison | Peer claims are checked from decoded paths on both runtimes. Codex routes a contested patch through its review queue, including the holder's name in the reason. Pre-tool snapshots record each decoded path's content digest. Post-tool observation claims and diagnoses only paths whose actual content changed, including partial failures; an unchanged failed patch cannot claim paths or repair notes. |

`policy_root` selects the application's policy source; native `cwd` still
anchors relative operands, repository membership and the review queue. A
temporary workspace does not become another checkout of that application.
Generated-hook regressions verify that absolute writes back to the
application's human-owned files are stopped on both runtimes, by the
destination's own gate naming the file's author, and that a repository holding
no grant meets the foreign-repository ask instead. Where the call is put to a
receipt rather than a request, the target's exact preimage is bound to it.
Application-opened sessions include `uv run --project <policy_root>` in operator
recovery commands, selecting the application's CLI while keeping the native
working directory and its review queue. Those answer commands remain operator-only.

Native hook discovery warnings are surfaced separately from readiness errors.
A Codex 0.155.1 probe against a disposable home and external working directory
loaded the declared hooks and reported that `SessionEnd` timeout is clamped to
three seconds. That native ceiling does not disable the hook. Missing declared
hooks, unresolved load errors and untrusted hooks refuse session startup;
skill-only plugins do not require hook evidence.

Neither runtime remembers an approval. An observed execution records that it ran and nothing more, so every ask re-enters review on both. A queue answer releases one exact retry: the receipt binds the captured documents, resolved paths, origin policy and accepted destination policies. `test_codex_review_delivery.py` verifies native notification, blocking, independent settlement and one-use replay using an inert local Responses server.

`uv run lup-devtools dev questions serve` provides the same persistent browser
inbox for durable review records from either runtime. One operator capability
protects its queue APIs; the same captured diffs, exact commands, approval and
rejection notes, and atomic answer transition apply to both. The command that
mints that capability is operator-only under both generated policies. Claude's
native permission requests remain native requests; the inbox displays what
was parked in the durable relay. Browser settlement records the answer before
returning to the browser; notification continues in the server, and its
failure does not undo the decision.
Queue acceptance alone does not prove that a particular recipient took a turn.
The Codex idle-wake integration test measures the complete browser-to-relay
path on CLI 0.156.1. The fixture forwards its declared launch identity into a
real native stdio child. Its owned heartbeat recovers a pulse aged beyond the
120-second presence window while preserving the hook-bound route. A completed
first turn is followed by an autonomously queued review
notification and a second completed turn carrying the operator's nonce to an
inert local Responses endpoint. It uses the recorded home and normal native
Unix socket discovery, without manually resuming the thread. Native
approval releases an exact retry through that runtime's existing hook path;
the inbox does not execute the operation itself.

## Parity audit of generated artifact families

Every family in `.claude/` vs `.codex/`/`.agents/`, with an explicit decision.

| Family | Claude | Codex | Decision |
| --- | --- | --- | --- |
| Skills (39) | `commands/*.md` | `skills/*/SKILL.md` | Parity — same 39 declarations, native formats. |
| Agents (4) | `plugins/lup/agents/*.md` | `.codex/agents/*.toml` | Parity — same 4 declarations, native formats. |
| Plugin manifest | `.claude-plugin/plugin.json` + marketplace | `.codex-plugin/plugin.json` + `.agents/plugins/marketplace.json` | Parity — native schemas. |
| Hooks (`hooks.json`, `scripts/policy.py`, `runtime/kernel.py`, `runtime/policy_data.py`, `runtime/evidence.json`) | Structured decisions, edit inspection, autonomous identities | Structured native permission decisions, per-file patch inspection, explicit review-queue approval for exact retries, declared identities from the environment or native payload | Shared semantic kernel and edit gates; approval presentation differs as stated above. Pending native permission events are never approval evidence. |
| Guidance | `.claude/CLAUDE.md` | `AGENTS.md` + `.codex/config.toml` | Parity — one document, native locations. |
| Ownership proof | `.claude/.lup-ownership.json` | `.codex/.lup-ownership.json` | Parity — same mechanism per tree. |
| Tool servers | `plugins/lup/.mcp.json` | `[mcp_servers.*]` in `.codex/config.toml` | Parity — same declarations, native schemas. A server declared `always_load` renders `alwaysLoad: true` on Claude, whose tool search defers MCP tools until searched for; Codex's per-server keys (https://learn.chatgpt.com/docs/extend/mcp) name no loading control and no deferral, so nothing renders there. |
| Template guidance | `TEMPLATE_CLAUDE.md` | `TEMPLATE_AGENTS.md` | Parity — shared portable sections, platform slices per flavor. |
| Resolver entry | skill instructs the CLI directly | skill instructs the CLI directly | Parity — neither tree generates a launcher artifact; the shared `resolve --adapter <runtime>` CLI is the entry on both sides. |
| `docs/` | rendered by the Claude recipe | none | Intentional single copy — repository documentation at a neutral location, which neither runtime reads from its own tree. Each page renders identically under both prompt renderers, so a second copy would be a byte duplicate and would additionally give two ownership manifests the same paths to manage. The set is declared once in `content/docs/catalog.py`. |
| `settings.json` | `.claude/settings.json` | none | Intentional — Claude-native project settings (plugin enablement, marketplace, permissions, file suggestion). The Codex counterparts are the generated `.codex/config.toml` plus uncommitted personal `config.local.toml`. |
| `scripts/file_suggest.sh` | `.claude/plugins/lup/scripts/file_suggest.sh` | none | Intentional — wired to Claude's native `fileSuggestion` setting; Codex has no equivalent feature. |
| Codex-only files | none | `.codex/config.toml`, `.agents/plugins/marketplace.json` | Intentional — native Codex requirements with no Claude analogue (Claude's marketplace lives inside `.claude/plugins/`). |

## Parity audit of runtime capability families

| Family | Required evidence |
| --- | --- |
| Launch readiness | Exercise the same native startup path a user runs, including implicit services; `Ready` may describe only checks that completed successfully. |
| Authentication | Ask the runtime that owns and refreshes a credential, and cover every user-visible service whose authentication path differs from the primary model transport. Never infer managed-auth readiness from one locally decoded token. |
| Host bridges | Probe the native API each runtime actually calls for clipboard, browser, terminal, and credential access; a command shim proves only callers of that command. Advertised access must match the probe. |
| Delegated-agent paths | Invoke each declared role through every supported runtime with provider-neutral model tiers and tool capabilities; listing a tool or rendering a declaration does not prove the delegated turn can start. |
| Diagnostics and tests | Equivalent failures identify the failed capability, owning runtime, recovery, and affected credential or bridge without exposing secrets. Unit fixtures cover native adapters, and a live requirement exercises each supported startup path. |

### Outer-sandbox clipboard and login handoff

Both runtimes reach the same bounded clipboard broker. Claude Code's command
clients use the image's `xclip` and `wl-paste` shims directly. Codex's native
X11 client uses a private Xvfb server and a python-xlib selection bridge; its
installed CLI has no configurable clipboard-helper command. The composition
declares that transport, not a provider-name branch in the shared launcher.
Only native-X11 sessions start the display, with GLX and TCP disabled and a
per-session Xauthority cookie. Neither transport mounts a host display socket.
The wrapper waits for server and selection ownership before starting the CLI,
then cleans them up with it. Large native transfers are incremental and bounded;
unsupported types are refused, not truncated or forwarded to the desktop.

Regression tests exercise command and native clients against a synthetic
clipboard, including live changes, text copies, image reads, payload limits,
and private-display authentication. Isolated Codex 0.153.4 TUI probes attached
a synthetic PNG through its actual paste shortcut on the host and in the built
image, without a model request or real credentials; the container probe had no
network. This proves the native clipboard path, independently of authentication.
Host-broker discovery reports
only its own result, and private-display startup is checked inside the session.

Host login handoff fingerprints the selected credential fields. A changed host
login replaces those fields once; an unchanged host login preserves the
container's renewed tokens. Claude's shared credentials file retains unrelated
MCP credentials, and Codex's dedicated auth file is replaced as a unit. A
fingerprint is bookkeeping, not authentication evidence: the owning runtime
must still validate or renew its credential in the session's actual boundary.

A separate Codex 0.153.4 outer-container probe successfully forced managed
login renewal through `account/read(refreshToken=true)`. An ephemeral thread
using that saved login then observed `codex_apps` startup go from `starting`
to `ready`. No model turn or app tool call ran. This checks the implicit
service's own authentication path, not merely the primary account response;
it is evidence for the selected account and runtime, not a promise of future
remote-service availability.

## What portable prose may name

Nothing a runtime spells for itself. A skill that means the guidance file, a
settings location, a plugin directory, a model tier, or the runtime's own
documentation says so through a typed part, and the adapter supplies the word:
`scope="this_tree"` when the sentence instructs the reader to act on their own
tree, `scope="every_tree"` when it describes what the repository holds and both
trees must be named. `reject_native_prose` refuses the rest at compile time, so
this is an invariant rather than a convention.

Two things prose may still name, because they are not platform facts. Tool
grants stay literal: `ToolGrant` (`packages/lup/src/lup/types.py`) deliberately
adopts one tool vocabulary for every runtime, so a skill teaching which grants
to declare names them as the closed type spells them. And the SDK symbols in
the guidance's Type Safety section stay literal too — they are importable names
from the library this template builds on, needed verbatim by a reader on either
runtime.
